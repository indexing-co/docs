---
title: Overview
description: The quick start for working with Neighborhoods
---

<Warning>
These docs are a work in progress!
We'll be rapidly updating them in coming days to add more guides and clearer descriptions.
Reach out any time for questions 🐇
</Warning>

# Overview
Neighborhoods are distributed clusters of nodes at your service. The primary way to work with them today is via pipelines.

Each pipeline consists of:
1. List of [networks](/networks) to subscribe to
2. Filter (e.g. wallet addresses)
2. Transformation step (JavaScript)
3. Destination (Postgres, Webhooks)

# Authentication

All API endpoints are authenticated using an authentication header, `X-API-KEY`.

<Note>
To get a new key, reach out to [hello@indexing.co](mailto:hello@indexing.co) or find us on [Farcaster](https://warpcast.com/runninyeti.eth).
</Note>

# OpenAPI Spec
<Card
  title="Neighborhood Endpoints"
  icon="leaf"
  href="https://github.com/indexing-co/docs/blob/main/api-docs/openapi.json"
>
  View the OpenAPI specification file
</Card>
